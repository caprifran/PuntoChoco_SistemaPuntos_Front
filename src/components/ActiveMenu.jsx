import React from "react";
import { useLocation } from "react-router-dom";

const ActiveMenu = () => {
  const location = useLocation();

  return (
    <div id="actiMenuContainer">
    </div>
  );
};

export default ActiveMenu;
